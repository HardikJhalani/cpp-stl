#include <bits/stdc++.h>
using namespace std;

int main() {

    unordered_set<int> st;

    st.insert(2);
    st.insert(1);
    st.insert(2);

    // average time complexity  is 0(1)
    // but the worst case is linear in nature, 0(set size)


    return 0;
}